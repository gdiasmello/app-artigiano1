Como rodar localmente
Pré-requisitos: Ambiente de execução de JavaScript instalado no seu computador.

Instale as dependências do projeto:
npm install

Configure a sua chave de API diretamente no arquivo .env.local.

Inicie o aplicativo:
npm run dev
